[pytest]
testpaths = tests
asyncio_mode = auto
# One event loop for the whole run: the async SQLAlchemy engine is created
# once at import and its pooled aiosqlite connections are bound to a loop.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
filterwarnings =
    ignore::DeprecationWarning
